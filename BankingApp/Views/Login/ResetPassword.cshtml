@model BankingApp.Core.Application.ViewModels.User.ResetPasswordViewModel

@{
    ViewData["Title"] = "Restablecer Contraseña";
    Layout = "_LayoutAnonymous";
}

<div class="container d-flex align-items-center justify-content-center min-vh-100">
    <div class="col-12 col-sm-10 col-md-8 col-lg-5 col-xl-4">
        <div class="card shadow-lg border-0 rounded-4">
            <div class="card-header bg-primary text-white text-center py-3 rounded-top-4">
                <h5 class="mb-0">🔒 Restablecer Contraseña</h5>
            </div>

            <div class="card-body p-4">
                <div class="alert alert-info rounded-4 shadow-sm mb-4" role="alert">
                    <strong>Requisitos:</strong>
                    <ul class="mb-0 mt-2 text-start">
                        <li>Mínimo 6 caracteres</li>
                        <li>Ambas contraseñas deben coincidir</li>
                    </ul>
                </div>

                @if (Model?.HasError == true)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        @Model.Error
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                }

                <form asp-controller="Login" asp-action="ResetPassword" method="post">
                    <input type="hidden" asp-for="UserId" />
                    <input type="hidden" asp-for="Token" />

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label">Nueva Contraseña</label>
                        <input asp-for="Password" type="password" class="form-control" placeholder="Ingresa tu nueva contraseña" />
                        <span asp-validation-for="Password" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ConfirmPassword" class="form-label">Confirmar Nueva Contraseña</label>
                        <input asp-for="ConfirmPassword" type="password" class="form-control" placeholder="Confirma tu nueva contraseña" />
                        <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 py-2 mt-2">
                        Restablecer Contraseña
                    </button>

                    <a asp-controller="Login" asp-action="Index" class="btn btn-outline-secondary w-100 py-2 mt-3">
                        Volver al inicio de sesión
                    </a>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
